version: "3.9"

services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: ampersand
      POSTGRES_PASSWORD: ampersand
      POSTGRES_DB: ampersand
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ampersand"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
